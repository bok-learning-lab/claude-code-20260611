# Node
node_modules/
*.log
# dist/ is committed on purpose: the repo .mcp.json points at dist/index.js,
# so the server must work straight from a clone without pnpm install && pnpm build.

# Environment / secrets
.env
.env.*
!.env.example

# MCP config (may contain API keys) — share .mcp.json.example instead
.mcp.json

# macOS
.DS_Store

# Editors
.idea/
.vscode/
*.swp
