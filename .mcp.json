{
  "_instructions": "WORKSHOP SETUP: replace this file's entire contents with the configuration from the Day 4 Google Doc (it contains the API keys, which we never publish in the repo). Then restart your Claude session, approve the servers when prompted, and run /mcp to confirm. The Python servers also need a one-time build: just ask Claude to run 'bash mcps-apis/setup.sh'.",
  "mcpServers": {}
}
